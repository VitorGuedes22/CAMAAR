<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gerenciamento resultados</title>
  </head>
  <body>
    <%= render 'layouts/header' %>
   <form class="bg-white w-1/2  mx-auto p-12 mt-3 flex flex-col gap-4">
      <%= render 'layouts/radio_form' %>
      <%= render 'layouts/text_form' %>

      <div class="flex justify-end w-11/12 mt-5">
        <button class="bg-green-500 px-9 rounded-full hover:bg-green-600">Enviar</button>
      </div>
    </form>
  </body>
</html>